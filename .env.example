# Copy this file to .env.local and add only the providers you plan to test.
# The local Quality Lab service reads these values; the browser never does.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
