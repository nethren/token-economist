# The application itself needs no credential. It never calls a model provider.
#
# This single key is only for the opt-in token-calibration check
# (`npm run eval:live`), which sends fixture texts — never your data — to
# Anthropic's free count_tokens endpoint to verify the offline estimate.
# It is read from the shell environment, not from this file.
ANTHROPIC_API_KEY=
